id: apparel-v1
version: 1.0.0
categories:
  - tshirt
  - hoodie
  - tote
  - jeans
  - sneakers
json_key_language: en
description: "AI-curated defaults & rules for apparel v1 (EU focus)."
links:
  lci_map: "../../lci_map.csv"
contracts:
  dpp_schema_version: "1.0.0"   # odpovídá packages/schema/dpp.schema.json v1
exposed:
  defaults_dir: "./defaults"
  relevance_dir: "./relevance"
  vocab_dir: "./vocab"
assumptions:
  region: "EU"
  units: "SI"
  transparency:
    ai_estimate_flag: true
